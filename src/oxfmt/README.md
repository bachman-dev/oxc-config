> [!CAUTION]
> This file is auto-generated by [docs.ts](/scripts/docs.ts) and should not be edited directly.

# Oxfmt Configuration

Here is a rundown of each Oxfmt setting that we enforce to provide a consistent style across code, configuration files, documentation, and other text files in our codebases.

## [arrowParens](https://oxc.rs/docs/guide/usage/formatter/config-file-reference.html#arrowParens)

### Setting

```json
"always"
```

### Explanation

Promotes consistent arrow function style

## [bracketSameLine](https://oxc.rs/docs/guide/usage/formatter/config-file-reference.html#bracketSameLine)

### Setting

```json
false
```

### Explanation

Makes for clean-looking HTML(esque) tags

## [bracketSpacing](https://oxc.rs/docs/guide/usage/formatter/config-file-reference.html#bracketSpacing)

### Setting

```json
true
```

### Explanation

Ensures that object literals are easy to read when printed on one line

## [embeddedLanguageFormatting](https://oxc.rs/docs/guide/usage/formatter/config-file-reference.html#embeddedLanguageFormatting)

### Setting

```json
"auto"
```

### Explanation

Code examples in Markdown end up well-formatted

## [endOfLine](https://oxc.rs/docs/guide/usage/formatter/config-file-reference.html#endOfLine)

### Setting

```json
"lf"
```

### Explanation

Complies with UNIX-like OS conventions, and ensures Windows development doesn't use incorrect line endings

## [experimentalOperatorPosition](https://oxc.rs/docs/guide/usage/formatter/config-file-reference.html#experimentalOperatorPosition)

### Setting

```json
"end"
```

### Explanation

Logical operations read more naturally (eg "if... AND"

## [htmlWhitespaceSensitivity](https://oxc.rs/docs/guide/usage/formatter/config-file-reference.html#htmlWhitespaceSensitivity)

### Setting

```json
"css"
```

### Explanation

Prevents HTML rendering issues

## [ignorePatterns](https://oxc.rs/docs/guide/usage/formatter/config-file-reference.html#ignorePatterns)

### Setting

```json
["dist/**"]
```

### Explanation

Sensible default for most Bachman Dev projects which build to a dist folder

> [!TIP]
> You can specify additional ignore patterns by passing this option as part of your own configuration object (see installation instructions).

## [insertFinalNewline](https://oxc.rs/docs/guide/usage/formatter/config-file-reference.html#insertFinalNewline)

### Setting

```json
true
```

### Explanation

POSIX compliance where tools expect files to end with a newline

## [jsdoc](https://oxc.rs/docs/guide/usage/formatter/config-file-reference.html#jsdoc)

### Setting

```json
{
  "addDefaultToDescription": false,
  "bracketSpacing": false,
  "capitalizeDescriptions": true,
  "commentLineStrategy": "singleLine",
  "descriptionTag": false,
  "descriptionWithDot": false,
  "keepUnparsableExampleIndent": false,
  "lineWrappingStyle": "greedy",
  "preferCodeFences": true,
  "separateReturnsFromParam": false,
  "separateTagGroups": false
}
```

### Explanations

- `addDefaultToDescription: false` as default values belong in the function signature; the `jsdoc/no-defaults` oxlint rule bans the `[name=default]` syntax this option reads from
- `bracketSpacing: false` keeps parameter specifications compact
- `capitalizeDescriptions: true` is a grammatical choice, as our descriptions are often sentences/paragraphs
- `commentLineStrategy: "singleLine"` keeps the comments compact
- `descriptionTag: false` as we prefer plain paragraphs instead of using a tag for our descriptions
- `descriptionWithDot: false` so as to not clash with a default parameter if present
- `keepUnparsableExampleIndent: false` to help call out syntax issues in code examples
- `lineWrappingStyle: "greedy"` to help keep documentation compact and concise
- `preferCodeFences: true` for consistent code examples even when a language isn't specified
- `separateReturnsFromParam: false` and `separateTagGroups: false` to help keep the documentation compact

## [jsxSingleQuote](https://oxc.rs/docs/guide/usage/formatter/config-file-reference.html#jsxSingleQuote)

### Setting

```json
false
```

### Explanation

Use double quotes when possible consistent with the rest of a project

## [objectWrap](https://oxc.rs/docs/guide/usage/formatter/config-file-reference.html#objectWrap)

### Setting

```json
"preserve"
```

### Explanation

Allows for multi-line objects when intended to be human-readable

> [!TIP]
> If you wish to have an object (e.g. a JSON file) kept to one line, either ignore the file completely using [ignorePatterns](https://oxc.rs/docs/guide/usage/formatter/config-file-reference.html#ignorepatterns), or specify [overrides](https://oxc.rs/docs/guide/usage/formatter/config-file-reference.html#overrides) to collapse it if it will fit within the print width

## [overrides](https://oxc.rs/docs/guide/usage/formatter/config-file-reference.html#overrides)

### Setting

```json
[]
```

### Explanation

Overrides are usually set at the project level

> [!TIP]
> You can specify project-specific overrides by passing this option as part of your own configuration object (see installation instructions).

## [printWidth](https://oxc.rs/docs/guide/usage/formatter/config-file-reference.html#printWidth)

### Setting

```json
120
```

### Explanation

Prevents excessive line wraps and is suitable for modern widescreen displays

## [proseWrap](https://oxc.rs/docs/guide/usage/formatter/config-file-reference.html#proseWrap)

### Setting

```json
"preserve"
```

### Explanation

Leans on soft-wraps in the editor while allowing for hard-wraps when needed such as blockquotes

## [quoteProps](https://oxc.rs/docs/guide/usage/formatter/config-file-reference.html#quoteProps)

### Setting

```json
"as-needed"
```

### Explanation

Keeps object property names neat while allowing for quotes on the rare occasion it is needed

## [semi](https://oxc.rs/docs/guide/usage/formatter/config-file-reference.html#semi)

### Setting

```json
true
```

### Explanation

Provides a visual indicator for the end of a statement in JavaScript

## [singleAttributePerLine](https://oxc.rs/docs/guide/usage/formatter/config-file-reference.html#singleAttributePerLine)

### Setting

```json
true
```

### Explanation

Makes HTML(esque) attributes readable without excessive scrolling

## [singleQuote](https://oxc.rs/docs/guide/usage/formatter/config-file-reference.html#singleQuote)

### Setting

```json
false
```

### Explanation

Generally requires less escaping (e.g. apostrophes) vs the need to escape double quotes

## [sortImports](https://oxc.rs/docs/guide/usage/formatter/config-file-reference.html#sortImports)

### Setting

```json
{
  "customGroups": [],
  "groups": [
    "side_effect",
    { "newlinesBetween": true },
    "wildcard-builtin",
    "builtin",
    { "newlinesBetween": true },
    "wildcard-external",
    "external",
    { "newlinesBetween": true },
    ["wildcard-internal", "wildcard-subpath"],
    ["internal", "subpath"],
    { "newlinesBetween": true },
    ["wildcard-parent", "wildcard-sibling", "wildcard-index"],
    ["parent", "sibling", "index"],
    { "newlinesBetween": true },
    "style",
    "unknown"
  ],
  "ignoreCase": false,
  "internalPattern": ["~/", "@/", "#"],
  "newlinesBetween": false,
  "order": "asc",
  "partitionByComment": false,
  "partitionByNewline": false,
  "sortSideEffects": false
}
```

### Explanations

- No custom groups currently
- We group the imports into side-effects, built-ins, external packages, internal packages, and file path imports; style and unknown type imports are put at the bottom
- `ignoreCase: false` to avoid blending upper and lowercase imports
- `newlinesBetween: false` at the top level so that we only separate the bundles of groups instead of each individual group with a newline
- `order: "asc"` to sort alphabetically from A-Z
- `partitionByComment: false` and `partitionByNewline: false` as we don't want to use partitions to affect sorting
- `sortSideEffects: false` for security purposes

> [!IMPORTANT]
> Currently oxfmt does not support using the import _specifiers_ as a sort key within the groups -- only the path. Should this feature land in a future oxfmt update, we intend to use the specifier as the sort key. In the meantime, we rely on the `eslint/sort-imports` oxlint rule to call out and help sort the specifiers as best we can.

## [sortPackageJson](https://oxc.rs/docs/guide/usage/formatter/config-file-reference.html#sortPackageJson)

### Setting

```json
{ "sortScripts": true }
```

### Explanation

Keeps package information organized, including script names

## [sortTailwindcss](https://oxc.rs/docs/guide/usage/formatter/config-file-reference.html#sortTailwindcss)

### Setting

```json
false
```

### Explanation

Generally set on a per-project basis

> [!IMPORTANT]
> When configuring a project that uses Tailwind, make sure to configure this in a configuration object passed to the method (see installation instructions), especially the `stylesheet` setting so oxfmt can find and manage your Tailwind CSS file.

## [svelte](https://oxc.rs/docs/guide/usage/formatter/config-file-reference.html#svelte)

### Setting

```json
false
```

### Explanation

Generally set on a per-project basis

> [!IMPORTANT]
> We generally don't use Svelte in any of our projects; it may be a mistake to enable this configuration.

## [tabWidth](https://oxc.rs/docs/guide/usage/formatter/config-file-reference.html#tabWidth)

### Setting

```json
2
```

### Explanation

Generally accepted indentation by the industry

## [trailingComma](https://oxc.rs/docs/guide/usage/formatter/config-file-reference.html#trailingComma)

### Setting

```json
"all"
```

### Explanation

Makes for cleaner Git diffs

## [useTabs](https://oxc.rs/docs/guide/usage/formatter/config-file-reference.html#useTabs)

### Setting

```json
false
```

### Explanation

Spaces are displayed more consistently across editors and GitHub

## [vueIndentScriptAndStyle](https://oxc.rs/docs/guide/usage/formatter/config-file-reference.html#vueIndentScriptAndStyle)

### Setting

```json
false
```

### Explanation

Keeping consistency with other HTML-esque attributes
